# =============================================================================
# MAAT-AI Environment Variables Template
# Copy this file to .env and fill in your values
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED - API Authentication
# -----------------------------------------------------------------------------
# API key for backend authentication (must match frontend VITE_API_KEY)
# Generate a secure random key: openssl rand -hex 32
MAAT_API_KEY=your-secure-random-api-key-here

# -----------------------------------------------------------------------------
# REQUIRED - NVIDIA NIM (NVIDIA Inference Microservices)
# -----------------------------------------------------------------------------
# Get your API key from: https://build.nvidia.com/
NVIDIA_NIM_KEY=your-nvidia-nim-api-key-here

# -----------------------------------------------------------------------------
# REQUIRED - Pinecone Vector Database
# -----------------------------------------------------------------------------
# Get your API key from: https://www.pinecone.io/
PINECONE_API_KEY=your-pinecone-api-key-here
# Pinecone index name for embeddings
PINECONE_INDEX=legal-rag
# Pinecone cloud provider (aws, gcp)
PINECONE_CLOUD=aws
# Pinecone region (e.g., "us-east-1", "gcp-starter")
PINECONE_REGION=us-east-1
# Embedding dimension (must match your embedding model output)
PINECONE_DIM=1024
# Distance metric: cosine, dotproduct, euclidean
PINECONE_METRIC=cosine

# Legacy variable names (deprecated but still supported)
# PINECONE_KEY (alias for PINECONE_API_KEY)
# PINECONE_INDEX_NAME (alias for PINECONE_INDEX)
# PINECONE_ENVIRONMENT (deprecated - use PINECONE_CLOUD + PINECONE_REGION)

# -----------------------------------------------------------------------------
# OPTIONAL - DuckDuckGo Search Configuration
# -----------------------------------------------------------------------------
# DDGS region for web search (default: "in-en" for India English)
DDGS_REGION=in-en
# Maximum results from DuckDuckGo search (default: 3)
DDGS_MAX_RESULTS=3

# -----------------------------------------------------------------------------
# OPTIONAL - Query Summarization Threshold
# -----------------------------------------------------------------------------
# Maximum query length before LLM summarization is invoked (default: 120)
QUERY_SUMMARIZE_THRESHOLD=120

# -----------------------------------------------------------------------------
# OPTIONAL - History Truncation
# -----------------------------------------------------------------------------
# Maximum tokens for chat history (default: 4000)
MAX_HISTORY_TOKENS=4000